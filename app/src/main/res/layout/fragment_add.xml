<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="?android:attr/windowBackground"
    tools:context=".navFragments.DiscoverFragment">

    <!-- ✅ Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/white"
        android:elevation="4dp"
        android:theme="?attr/actionBarTheme"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageButton
                android:id="@+id/backArrow"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Back"
                android:padding="8dp"
                android:src="@drawable/ic_arrow_back" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:text="Discover"
                android:textColor="@color/black"
                android:textStyle="bold"
                android:textSize="20sp"
                android:gravity="center"/>

            <ImageButton
                android:id="@+id/profileIcon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_marginEnd="13dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Profile"
                android:padding="8dp"
                android:src="@drawable/ic_profile" />

        </RelativeLayout>
    </androidx.appcompat.widget.Toolbar>

    <!-- ✅ Search bar -->
    <EditText
        android:id="@+id/searchRecipes"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/search_background"
        android:drawableEnd="@drawable/ic_search"
        android:hint="Search recipes..."
        android:padding="10dp"
        android:textColor="#000"
        android:textColorHint="#888"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ✅ Image grid -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/imageGrid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/searchRecipes"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Row 1 -->
        <ImageView
            android:id="@+id/createrecipebutton"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:src="@drawable/add_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/overview_button"
            app:layout_constraintHorizontal_chainStyle="spread"/>

        <ImageView
            android:id="@+id/overview_button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:src="@drawable/overview_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/createrecipebutton"
            app:layout_constraintEnd_toStartOf="@id/edit_button"/>

        <ImageView
            android:id="@+id/edit_button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:src="@drawable/edit_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/overview_button"
            app:layout_constraintEnd_toStartOf="@id/delete_button"/>

        <ImageView
            android:id="@+id/delete_button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:src="@drawable/delete_button"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/edit_button"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Row 2 -->
        <ImageView
            android:id="@+id/scale_recipe_button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/scale_recipe"
            app:layout_constraintTop_toBottomOf="@id/createrecipebutton"
            app:layout_constraintStart_toStartOf="@id/createrecipebutton"
            app:layout_constraintEnd_toEndOf="@id/overview_button"/>

        <ImageView
            android:id="@+id/view_recipe_button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_marginTop="16dp"
            android:src="@drawable/view_recipe"
            app:layout_constraintTop_toBottomOf="@id/overview_button"
            app:layout_constraintStart_toStartOf="@id/edit_button"
            app:layout_constraintEnd_toEndOf="@id/delete_button"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
